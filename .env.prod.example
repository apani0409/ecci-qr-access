# ==================================
# PRODUCTION ENVIRONMENT VARIABLES
# ==================================
# Copy this file to .env.prod and fill in the values
# DO NOT commit .env.prod to version control!

# Database Configuration
DB_NAME=ecci_control
DB_USER=ecci_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# Backend Security
SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
ENVIRONMENT=production
DEBUG=false

# CORS - Update with your actual domain
CORS_ORIGINS=https://ecci-control.com,https://www.ecci-control.com

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM_EMAIL=noreply@ecci-control.com
SMTP_FROM_NAME=ECCI Control System

# Frontend URL
FRONTEND_URL=https://ecci-control.com
API_URL=https://api.ecci-control.com

# Backup Configuration
BACKUP_RETENTION_DAYS=7

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# Optional: Redis (if using)
REDIS_URL=redis://redis:6379/0

# ==================================
# SSL Certificate Paths
# ==================================
# Place your SSL certificates in nginx/ssl/
# cert.pem - Your SSL certificate
# key.pem - Your SSL private key

# ==================================
# How to generate a strong SECRET_KEY:
# ==================================
# Run: openssl rand -hex 32
# This will generate a secure random key

# ==================================
# Gmail SMTP Setup:
# ==================================
# 1. Enable 2-factor authentication in your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an "App Password" for "Mail"
# 4. Use that password in SMTP_PASSWORD (not your regular Gmail password)

# ==================================
# SSL Certificate Setup:
# ==================================
# Option 1 - Let's Encrypt (Recommended):
# 1. Install certbot: sudo apt install certbot
# 2. Generate cert: sudo certbot certonly --standalone -d ecci-control.com
# 3. Copy files:
#    sudo cp /etc/letsencrypt/live/ecci-control.com/fullchain.pem nginx/ssl/cert.pem
#    sudo cp /etc/letsencrypt/live/ecci-control.com/privkey.pem nginx/ssl/key.pem
#
# Option 2 - Self-signed (Development only):
# openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#   -keyout nginx/ssl/key.pem -out nginx/ssl/cert.pem
