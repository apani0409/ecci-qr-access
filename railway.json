{
  "root": ".",
  "buildCommand": "pip install -r backend/requirements.txt",
  "startCommand": "cd backend && gunicorn --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT app.main:app"
}
